rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Permite que qualquer usuário autenticado leia, escreva e apague
    // arquivos dentro da pasta 'clientes'.
    match /clientes/{clientId}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}