rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permite que qualquer usuário LOGADO leia e escreva em qualquer coleção.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}